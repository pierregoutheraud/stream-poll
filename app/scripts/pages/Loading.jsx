import React from 'react';

var Loading = React.createClass({
  render: function() {
    return (
      <div className="loading">
        <p>Loading...</p>
      </div>
    );
  }
});

export default Loading;
